# Copy this file to `.env` and fill in your values.

# Required: Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=123:abc...

# Required: Supabase Postgres connection string
# Recommended for Vercel/serverless: use the "Transaction pooler" connection string (port 6543).
# Example (direct):  postgresql://postgres:<password>@db.<project-ref>.supabase.co:5432/postgres?sslmode=require
# Example (pooler):  postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require
DATABASE_URL=postgresql://...

# Optional: restrict bot to specific Telegram user ids (comma-separated)
# TELEGRAM_ALLOWED_USER_IDS=123456789,987654321
TELEGRAM_ALLOWED_USER_IDS=

# Optional: verify Telegram webhook requests (recommended).
# When set, configure your webhook with the same `secret_token` so Telegram includes
# `X-Telegram-Bot-Api-Secret-Token` on each request.
TELEGRAM_WEBHOOK_SECRET_TOKEN=

# Optional: INFO, DEBUG, WARNING, ERROR
LOG_LEVEL=INFO
